---
name: template-skill
description: Replace with a description of what this skill does and the specific contexts in which Claude should use it. This text is the primary trigger — put the key use case first; the combined description is truncated at 1,536 characters in the skill listing.
---

# Insert instructions below

<!--
Authoring notes (delete before shipping):

- Keep this body under ~500 lines. Move optional, lookup, or rarely-needed
  detail into a `references/` folder beside this file and link to it; Claude
  loads those files only when a task needs them (progressive disclosure).
- Put deterministic, reusable code in a `scripts/` folder and tell Claude
  whether to run it or read it. Put templates/assets in an `assets/` folder.
- Optional frontmatter fields: `disable-model-invocation: true` (manual-only,
  for side-effecting workflows), `allowed-tools` (pre-approve tools),
  `when_to_use`, `paths`. See https://code.claude.com/docs/en/skills
-->
