# Sync state cache: full last-synced base bodies + per-page sync metadata.
# Local, machine-specific, and rebuildable from a fresh pull — never committed.
confluence/**/.sync/
.sync/

# Pulled Confluence vault content (mirror data, not skill source)
confluence/
